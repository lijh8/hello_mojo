{
    "tasks": [
        {
            "type": "shell",
            "label": "build",
            "command": "pixi run mojo build -g ${file}",
            "args": [],
            "options": {
                // "cwd": "${workspaceFolder}/src",
                "cwd": "${fileDirname}",
            }
        }
    ],
    "version": "2.0.0",
}
